PORT=3001
DATABASE_URL=postgresql://localhost:5432/priority_focus_manager
JWT_SECRET=your-secret-key-change-this-in-production
NODE_ENV=development

# Production settings (for Railway/Render/etc):
# NODE_ENV=production
# DATABASE_URL will be auto-set by hosting provider
# JWT_SECRET should be a secure random string (32+ characters)
